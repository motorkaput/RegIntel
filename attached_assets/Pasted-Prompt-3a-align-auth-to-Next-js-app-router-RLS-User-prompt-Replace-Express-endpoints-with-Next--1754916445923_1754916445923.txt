Prompt 3a — align auth to Next.js app router + RLS
User prompt:
Replace Express endpoints with Next.js 14 route handlers under src/app/api/**. Keep the same functionality but:

Use httpOnly cookie JWT on login/register/logout.

Add lib/db/rls.ts with withRLS(prisma,{tenantId,role,userId},fn) that sets:
select set_config('app.current_tenant_id', $1, true);
select set_config('app.current_role', $2, true);
select set_config('app.current_user_id', $3, true);

Wrap every DB call inside withRLS in all route handlers.

Implement magic link:
POST /api/auth/magic-link (generate + email via Postmark)
GET /api/auth/magic-link/verify (consume token, issue JWT)

Implement invitations:
POST /api/invitations/create (admin only)
POST /api/invitations/accept (token → create/update user, set password, issue JWT)

Middleware: src/middleware.ts guards /(dashboard) and /api/* (except /api/auth/* and invitation accept). Redirect to /login if unauthenticated.

Audit log auth events.

Remove Express server code and dependencies.

Update docs/api.md to reflect Next.js route handlers and withRLS requirement.

Acceptance:

No Express server left.

All protected routes fail without JWT.

Magic link and invitations work and send via Postmark.

All DB reads/writes happen inside withRLS.

Prompt 4 — implement two-tier sticky shell + footer (exact match)
User prompt:
Create the UI shell that exactly matches the Fetch Patterns app:

Components: src/components/layout/HeaderCompany.tsx, HeaderApp.tsx, FooterCompany.tsx; shared Layout at src/app/(dashboard)/layout.tsx.

Use the confirmed classnames and structure:
HeaderCompany: fixed top-0 left-0 right-0 z-50, backdrop-blur-sm, section-divider border-b border-gray-200, max-w-7xl container, etc.
HeaderApp: secondary sticky bar with the exact classes from the confirmation note.
FooterCompany: grid, typography, links, and copyright line exactly:
Copyright. Dark Street. All rights reserved. darkstreet.consulting

Navigation:
Top header (company): Products {Fetch Patterns, PerMeaTe Enterprise}, What’s next, Company, Contact.
Bottom header (app): start, analysis, breakdown, org upload, auto-assignment, dashboards, admin. Route-aware active states.

Mobile: same sticky behavior, scroll shadow, skip-to-content link, keyboard focus.

Do not change logo assets; only labels/links differ.

Add unit/visual tests for sticky behavior and active nav.

Acceptance:

Both headers are sticky with the same spacing/scroll shadow as the reference.

Footer matches exactly.

Headers appear on all /(dashboard) pages and reflect auth state (user chip, sign out).